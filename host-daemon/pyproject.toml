[project]
name = "moss-host-daemon"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0", "tomli-w>=1.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.21"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
